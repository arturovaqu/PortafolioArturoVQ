export default function Skills() {
  return (
    <section>
      {/* TODO: "MY CAPABILITIES" + bio + tags (HTML, CSS, JavaScript, jQuery, Accessibility, Figma, Tailwind CSS) */}
    </section>
  )
}
