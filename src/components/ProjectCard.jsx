export default function ProjectCard({ title, description, year, role, client, image, liveDemo, github, viewProject }) {
  return (
    <article>
      {/* TODO: imagen izquierda + info derecha (title, description, meta, links) */}
    </article>
  )
}
