export default function Contact() {
  return (
    <section>
      {/* TODO: "LET'S CONNECT" + email/resume izquierda + iconos sociales + formulario derecha (Name, Email, Subject, Message, SUBMIT) */}
    </section>
  )
}
